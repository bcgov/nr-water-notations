# Default values for water-notations.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
      - path: /
        backend:
          serviceName: chart-example.local
          servicePort: 80
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# -------------------------------------------------------------
app_name: waternote
env: dlv
label_database_role: db
label_api_role: app


# database image info:
#  https://catalog.redhat.com/software/containers/rhscl/postgresql-13-rhel7/60af640142119b927780b520?container-tabs=overview
postgresdb_params:
  database_secret_name: waternote-db-secrets
  database_name: fwapg
  # https://codersociety.com/blog/articles/helm-best-practices#11-use-the-lookup-function-to-avoid-secret-regeneration
  # password wil be autogenerated
  database_password: password123abc
  database_user: postgres
  database_pvc_size: "30Gi"
  database_port: 5432
  database_pvc_name: waternote-db-pvc

artifactory_secret_name: artifacts-pull-artifacts-default-nncfox

demojob:
  demojob_pvc_name: demojob-pvc
  demojob_pvc_size: "30Gi"

dataloadjob:
  job_name: dataload
  imagetag: 20230927-2010
  dataload_pvc_name: dataload-pvc
  dataload_pvc_size: "30Gi"

notationsjob:
  job_name: notations
  imagetag: 20220427-0858

artifactory:
  sa_name: artifactory-sa
  sa_descriptor: "My artifact service account"

objectstorage:
  secret_name: object-storage
